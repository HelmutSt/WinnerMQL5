{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "Compile MQL5",
      "type": "shell",
      "command": "${workspaceFolder}\\compile.bat",
      "args": [
        "${file}"
      ],
      "group": "build"
    }
  ]
}